{
  "name": "mqtt_demo_svr",
  "version": "0.1.0",
  "description": "Static HTTP file server with Socket.io <-> MQTT bridge used to demonstrate how to expose MQTT to the browser",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [
    "mqtt",
    "socket.io",
    "node.js"
  ],
  "author": "Pierce Primm, Sr.",
  "license": "MIT",
  "dependencies": {
    "socket.io": "^1.3.5",
    "mqtt": "^1.1.3"
  }
}
